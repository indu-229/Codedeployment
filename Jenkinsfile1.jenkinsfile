node{
    def buildImg
    checkout scm
    stage('Build Image')
    {
    buildImg=docker.build("indu12/myimg")
    }
    stage('Push Image')
    {
    docker.withRegistry('https://registry.hub.docker.com','dockerhub')
    buildImg.push()
    }
}
