properties([parameters([choice(choices: ['Job-in-slave', 'Job-in-slave2'], description: 'Select job to give access', name: 'Job')])])
def test
node{
  stage('Checkout') {
    checkout scm
  }

  stage('Load') {
    test = load 'job.groovy'
  }

  stage('Execute') {
    test.rest(job)
  }
}
